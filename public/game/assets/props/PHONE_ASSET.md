# Phone prop

`phone.glb` is an original project asset generated from
`scripts/blender/create_phone_prop.py`. It includes no third-party geometry or
textures and has no external runtime dependency.

The runtime places it between the avatar's normalized hand bones during the
texting/Photo pose animation and turns the screen toward the avatar while
keeping the body upright.
